## 1. CSS Custom Properties

- [x] 1.1 Add `--wb-palette-background`, `--wb-palette-border-radius`, `--wb-palette-padding`, `--wb-palette-gap` to `:host` block in `wb-palette.css` with current hardcoded values as defaults
- [x] 1.2 Update `.panel` rules to use `var()` references with fallback to the `:host` defaults

## 2. Documentation

- [x] 2.1 Update `readme.md` with a CSS Custom Properties table listing name, fallback, and description for each var
