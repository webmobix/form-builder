{
  "name": "@webmobix/form-core",
  "version": "0.0.1",
  "description": "Framework-agnostic engine: JSON Schema + UI Schema parsing, ajv validation, conditional rules. No DOM.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist/"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  }
}
